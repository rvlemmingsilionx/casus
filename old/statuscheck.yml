- name: Check NFS server status
  command: systemctl nfs-kernel-server status
  ignore_errors: yes
  changed_when: false
  register: service_nfs_status
  when:
    "'nfsservers' in group_names"

- local_action: copy content={{ service_nfs_status }} dest=/home/ansibleuser/output
